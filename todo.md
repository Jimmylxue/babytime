# 育娃手记 · 产品路线图（Todo）

> 核心目标：**增加用户（获客）+ 留住用户（留存）**，不是一味堆功能。
> 创建时间：2026-08-28 · 基于当时数据：累计用户 87 / 近7日新增 +20 / 近7日活跃 19 / 家庭成员 9 / AI分析 1 次 / 照片 22

## 数据诊断（为什么这么排）

- 近7日活跃 19 / 累计 87 ≈ **73% 用户一周内流失** → 最大的洞是「没有回访理由」
- 全仓库无订阅消息、无任何提醒 → 小程序唯一的主动唤醒通道是空的
- 87 用户只有 55 个宝宝 ≈ 1/3 注册完没建宝宝档案 → **激活流程缺失**
- 家庭共享 9 人 / 87 用户 → 内置裂变环基本没跑起来（邀请码摩擦太大）
- AI 便便分析全量仅 1 次 → 「AI 惊喜点」不构成留存理由，暂不加码
- 核心记录闭环成立（活跃用户日均记十几条）→ **不要再加记录功能**

---

## P0 · 立刻做（直接提升留存）

- [ ] **1. 微信订阅消息提醒（疫苗到期提醒为先）**（2026-09-03 完成代码，待微信模板配置与真机验收）
  - [ ] 小程序后台申请订阅消息模板（疫苗提醒、每日回顾）
  - [x] 客户端接入 `requestSubscribeMessage`（疫苗时间轴明确按钮 + 保存疫苗记录后辅助触发；单日去重）
  - [x] 疫苗计划数据下沉到服务端（22 节点，默认提前 3 天，可配置）
  - [x] 服务端定时扫描与幂等发送（创建者和已加入家人均支持；默认 9 点）
  - [x] 可选晚间回顾：模板配置后首页显示授权入口，默认 20 点发送当日摘要
  - [x] 记录授权、发送、失败和消息点击；后台增加订阅与唤回看板
  - [x] 活跃归属增加 `actorUserId`，家庭成员代记不再算到创建者
  - [x] D1/D7/D30 改为走完观察期后的精确自然日回访
  - [ ] 微信公众平台申请模板并将模板 ID/关键词字段写入生产环境变量
  - [ ] 体验版真机验证授权、正式/体验环境发送和消息点击回流
- [x] **2. 新用户激活引导**（2026-08-28 完成）
  - [x] 新增 `pages/onboarding/index` 引导页：欢迎语 + 3 步说明（建档案→记日常→看成长）+ 宝宝表单（头像可选/名字/性别/生日）
  - [x] 创建成功后弹窗引导「记第一条」（跳喂奶记录页），记录完成返回自动进首页
  - [x] 登录成功后检测无宝宝 → 自动进入引导页（`redirectTo`，不留登录页在返回栈）
  - [x] 首页检测已登录无宝宝 → 每次 App 启动自动进一次引导页；之后显示虚线引导卡「为宝宝建立专属档案 · 去创建」
  - [x] 引导页支持「跳过，先随便逛逛」
  - [x] `babyStore.addBaby` 返回创建的宝宝（供引导页拿 babyId 跳记录页）
- [x] **2.1 示例数据去误导化**（用户反馈"没注册就看到小宝"以为是 bug，2026-08-28 完成）
  - [x] mock 宝宝「小宝」改名「示例宝宝」，从名字上杜绝"别人的数据"错觉
  - [x] 未登录时首页/统计页顶部加示例横幅：「👀 示例数据预览」+ 去登录按钮
  - [x] 首页未登录时宝宝名字旁加「示例」小徽章
  - [x] 统计页对"已登录但未创建宝宝"的用户同样显示示例横幅 + 去创建按钮
  - [ ] 待观察：重新发版后看用户是否还有"看到别人的宝宝"的反馈
- [x] **2.2 修复：引导页跳过后首页又自动跳回**（2026-08-28 完成）
  - [x] 原因：登录页 redirectTo 直接进引导页，首页 useDidShow 未执行过，"启动只跳一次"标记仍为 false
  - [x] 修复：标记抽到 `utils/onboarding.ts` 共享，引导页「跳过」时同步置位，首页不再重复跳
- [x] **2.3 UI 打磨：图标去 emoji 化 + 首页未建档状态合并**（2026-08-28 完成）
  - [x] 新增 `assets/icons/`：baby-face（粉/蓝两色）、parent、baby、family、agreement、privacy、logout，统一 Feather/Lucide 风格线性描边图标
  - [x] 首页未建档状态合并为一张引导卡（原"未添加宝贝"卡 + 引导卡重复），卡内带「去创建」按钮
  - [x] 首页/引导页/建档页/宝宝列表/登录页 Logo 的 👶👦👧 emoji 全部换 SVG；个人中心家长默认头像 👨‍🍼 换 SVG
  - [x] 修复登录页漏导入 Image 组件的隐患；补 `types/images.d.ts` 声明消除全项目 svg 导入类型报错
  - [ ] 个人中心未登录卖点区的 ☁️📈👨‍👩‍👧 仍是 emoji，视觉可接受暂保留
- [x] **3. 「添加到我的小程序 / 浮窗」引导**（2026-08-28 完成）
  - [x] 记录页每次新建记录成功后，storage 累计计数（`stats:cumulativeRecords`）
  - [x] 首页 useDidShow 检测：已登录 + 有宝宝 + 累计 ≥ 3 条 + 未弹过 → 弹引导浮层（`guide:addToMyMp:done` 标记只弹一次）
  - [x] 浮层样式：右上角白色气泡 + 朝上箭头指向胶囊按钮，文案引导「点··· → 添加到我的小程序」，点蒙层或按钮即关闭并标记

## P1 · 增长口子（P0 完成后做）

- [ ] **4. 家庭邀请改造：分享卡片替代邀请码**（2026-08-28 完成开发，待测试）
  - [x] 服务端：新增 `family_invites` 表（一卡多人模型，与成员解耦），邀请与成员分离
  - [x] 规则：有效期 7 天（`FAMILY_INVITE_EXPIRE_DAYS` 可配）+ 家庭上限 8 人 + 重新生成作废旧卡 + 兼容旧一次性邀请码
  - [x] 新接口 `GET /family/invite/info/:code`（落地页状态判断：expired/full/already_member/bound_other/own/invalid）
  - [x] 客户端：新增 `pages/family-join` 落地页（未登录→登录回流→确认页→加入→回首页；转发不断链）
  - [x] 家庭页改造：自动生成邀请卡 + 「微信分享邀请卡」按钮 + 有效期展示 + 重新生成（force）
  - [x] 登录页支持 `redirect=family-join&invite=CODE` 回流参数
  - [ ] 测试：本地各分支验证 + 体验版真机验收（见下方测试方案）
  - [ ] 目标指标：家庭成员数 / 宝宝数 从 9/55（≈0.16）提到 1:1.5
- [ ] **5. 成长曲线（WHO 百分位）**（2026-09-02 完成开发，待真机验收）
  - [x] WHO 标准数据：`utils/whoGrowthStandards.ts`，WHO 2006 LMS 表计算 P3/P10/P25/P50/P75/P90/P97，月度采样 0-36 月龄 × 男女 × 身高/体重（开源库 who-growth-standards，MIT，已校验官方参考值）
  - [x] `components/GrowthCurveChart` Canvas 组件：7 条百分位参考线（P3/P50/P97 标注）+ 宝宝实测点连线 + 最新值气泡 + 生长动画 + 标题水印（导出自带说明）
  - [x] 统计页身高体重页签新增「趋势 / 成长曲线」视图切换；成长曲线拉全量历史（stats 接口无 days 上限）
  - [x] 曲线图支持保存到相册/分享
  - [x] 图表通用导出：`utils/chartExport.ts`（Canvas 2 倍分辨率导出）+ 各图表卡片右上角「分享/保存」按钮（奶量、喂奶次数、尿布次数、睡眠时长、身高/体重趋势、成长曲线），相册权限被拒时引导去设置，分享菜单失败兜底为保存
  - [ ] 待办：36 月龄以上宝宝暂无参考曲线（页面有 WHO 范围说明）；后续可加头围曲线
  - [ ] 真机验收：保存到相册权限流程、showShareImageMenu 在低版本基础库的兜底
- [ ] **6. 宝宝日报 / 周报海报**（日报本体 2026-09-02 完成，待真机验收）
  - [x] 底座：Canvas 离屏海报链路全通（隐藏画布、2x 导出、保存/分享、海报设计语言、宝宝头像/品牌/小结卡组件）——单图表海报已上线（喂奶/奶量/尿布/睡眠/身高/体重/成长曲线）
  - [x] **日报海报**：`utils/dailyPoster.ts`（340×620 多指标版式：头部品牌+日期胶囊+宝宝名/性别/月龄 → 6 张指标卡 2 列网格（喂奶/睡眠/便便尿布/辅食/饮水/户外，含副信息）→「今日小结」卡自动文案 → 水印）
  - [x] 入口：首页「今日记录」标题行右侧「生成日报」按钮（一键分享；长图发宝妈群）
  - [x] 小结自动生成：如「今天喂了7次奶、睡了13小时30分、换了4次尿布，又是被好好照顾的一天～」；无记录日提示引导记录
  - [ ] 周报形态（可选，日报上线看反馈再定）
  - [ ] 配合 P0-1 的每日回顾订阅消息，晚间推送摘要（依赖订阅功能，已搁置待模板 ID）

## P2 · 锦上添花（有精力再做）

- [x] **7. 记录效率优化**（2026-09-03 完成，入口按用户反馈调整）
  - [x] 打开记录页自动预填本类型最近一条记录的高频字段（奶量/方式/时长、尿布状态、辅食名、饮水量、体温、用药），时间/照片/备注不预填；用户已手动编辑过的字段不被覆盖
  - [x] 记录页顶部「上次记录」提示条：显示上次摘要（如 奶粉 · 150ml · 15分钟）+「已按上次预填，可直接保存」
  - [x] 保存后停留本页：发生时间重置为当前、类型字段保留（即「按上一条继续」），照片/便便分析/备注清空；按钮变「再记一条」；编辑模式仍保存后返回
  - [x] 「再来一条」入口（迭代：曾放首页行内按钮，因视觉突兀按用户选择移到详情页）：记录详情页点任意记录 → ActionSheet「编辑/再来一条/删除」，以该条为模板新建（时间取现在）；睡眠/身高体重/疫苗不支持重复
- [ ] **8. 数据导出备份**
  - [x] **成长纪念册**（2026-09-18 完成，待真机验收）—— 把一段时间的记录排成一张可保存/分享的长图
    - 入口：「我的」→「宝贝与家庭」→「成长纪念册」，点击后拉起微信分享面板（可发给好友或存相册）
    - 范围：优先「上一个完整自然月」，该月无记录则回退「最近 30 天」
    - 内容：记录天数 / 喂奶次数+奶量 / 睡眠日均 / 尿布次数 / 身高变化曲线 / 照片墙 2×3 / 结语 / 小程序码
    - 新增 `utils/albumData.ts`（聚合）、`utils/albumPoster.ts`（Canvas 渲染，逻辑尺寸 340×1080）
    - ⚠️ 结语**只用数据事实**，不写「会翻身」这类发育里程碑 —— 数据里没有依据，说错反而伤信任
    - [x] 修复崩溃 `hFrom.toFixed is not a function`（2026-09-18）：根因是 `records.height/weight` 是
      `decimal` 列，mysql2 默认返回**字符串**，而 `getDetail` 返回的是原始实体展开、没做转换
      （`getStats` 里有 `Number()` 所以没事）。`albumData.ts` 加 `num()` 显式转换；
      `albumPoster.ts` 加 `fixed()` 兜底，避免一个脏字段让整张海报渲染失败
    - [x] 修复「这些瞬间只有一张照片」（2026-09-18）：范围内照片少时，固定 3×2 网格 + 虚线空框看着像坏了。
      改为——候选从 12 张提到 30 张；范围内优先、不足用最近照片补足到 6 张；
      照片墙按实际张数自适应（1 张=整行主图 / 2 张=一行两张 / 4 张=2×2）；
      画布高度随内容动态计算；缩略图加载失败自动退回原图重试
    - [x] 修复「成片很虚、颜色不正、发暗」（2026-09-18）：根因是 `canvasToTempFilePath` 的
      `destWidth` 默认 = `width × 屏幕像素密度`（官方文档原文），而 2d 画布不传 width 时取的是
      **位图宽度**（已经 dpr 放大过）—— 相当于位图被再插值放大 2~3 倍，照片内容发糊。
      改为导出显式 `destWidth/destHeight = 位图尺寸`（1:1 不重采样）+ 位图倍数固定 3x
      （不跟设备 dpr，开发工具里从 680px 提到 1020px）+ 海报照片改为原图优先、缩略图只做兜底
    - [x] 同类问题统一修复（2026-09-18）：全项目只有 `chartExport.ts` 一个导出点（已修），
      另有 `chartPoster`（统计页图表海报）/ `dailyPoster`（首页日报）两个渲染器仍按设备 dpr ——
      已统一抽成共享常量 `POSTER_RENDER_SCALE = 3`（`posterHelpers.ts`），三个海报导出均为 1020px 宽
    - [ ] 待真机验收：长图在 dpr=3 时导出 1020×3240，**超长画布可能导出失败**（本次唯一平台风险）；若失败则调小 `ALBUM_H` 或降为 2x 导出
    - [ ] 可选：画质硬上限在**上传环节** —— 照片走 `sizeType: ['compressed']`（微信压缩图），
      且不传 thumbnail。想再提画质要改上传链路（允许原图），会增加存储/流量，需拍板
  - [ ] CSV/Excel 导出记录
  - [ ] 照片打包下载
  - [ ] 记录越久越不敢走，建立长期信任
- [ ] **9. 成长里程碑**
  - [ ] 第一次翻身/长牙/叫妈妈等打卡项
  - [ ] 生成纪念海报可分享（纪念册已具备海报能力，可复用其渲染管线）
  - [ ] 目的：拉长产品生命周期（喂养记录 1 岁后自然衰减，里程碑撑到 3 岁）
- [ ] **10. 辅食清单与过敏标记**
  - [ ] 新食物观察 3 天机制（6-12 月龄痛点）
  - [ ] 与现有辅食记录衔接

## P3 · 远期考虑

- [ ] AI 育儿问答（已接入智谱 glm-4v-flash，扩展文字问答成本不高；但便便 AI 仅 1 次使用，先放着验证需求）
- [ ] 睡眠作息分析建议（等睡眠数据积累够了）
- [ ] 多宝家庭视图切换优化

## P4 · 明确不做（省下时间）

- ❌ 会员/支付 —— 87 用户谈不上变现；个人主体小程序开不了微信支付。周活过千再想
- ❌ 社区/UGC —— 审核成本高、冷启动难，拖死独立开发者
- ❌ 更多记录类型 —— 11 种已足够，再加是负收益
- ❌ App 版本 —— 小程序形态与产品天然匹配，别分散火力

---

## 北极星指标（上线后盯）

| 指标 | 现状 | 目标 |
|---|---|---|
| 7 日留存（近7日活跃/累计） | ≈ 28% | 40%+ |
| 订阅消息授权率 / 点击率 | 0 | 授权 50%+ / 点击 10%+ |
| 家庭成员数 / 宝宝数 | 9/55 ≈ 0.16 | ≥ 1.5 |
| 日报海报分享次数 | 无 | P1-6 上线后观察 |

## UI 打磨记录

- [x] 统计页身高/体重折线图改 Canvas 2D 绘制（平滑贝塞尔曲线 + 渐变填充 + 空心测量点 + 末点数值气泡 + 虚线网格），`components/LineChart`
- [x] 全局页面底色从米黄 #FFFDF9 调整为粉白 #FFFAF9（含导航栏/TabBar，共 25 处）
- [x] 统计页柱状图改 Canvas 2D 绘制（细圆角柱 + 顶部渐变 + 柱顶数值 + 星期标签），`components/BarChart`，喂奶/奶量/尿布/睡眠四图共用
- [x] 统计页总结卡片数值下增加「较昨日 ±x」对比（取前一日 summary 计算差值；昨日无数据或双零时不显示；支持 次数/奶量/间隔/体温/身高/体重/时长）
- [x] 修复柱状图 14/30 天视图无法横向滚动（Canvas 原生组件吞触摸事件，加透明蒙层冒泡给 ScrollView）
- [x] 折线图/柱状图增加绘制动画：折线从左向右生长（裁剪实现）+ 末点气泡淡入；柱子从左到右依次长出 + 数值淡入（requestAnimationFrame + 缓动，重绘时取消上一次动画）
- [x] 图表分享海报全面改版（对齐参考设计）：淡粉渐变底 + 柔光；大头像（白描边+粉光环）+「育娃手记」+slogan 双爱心；日期粉底胶囊；大标题 + 竖线分隔元信息行；白色图表卡片（柱状图带 y 轴刻度/单位/加粗柱）；**「本周小结」卡片**（文档图标 + 分类文案 + 双爱心装饰）；底部水印
- [x] 小结文案按图表类型自动生成（喂奶规律/奶量稳定/尿布干爽/睡眠时长/身高体重变化/成长曲线），7 天显示「本周小结」，其他显示「近N天小结」
- [x] 海报按区间自动切换图表形态：7 天 = 日粒度柱状图；14/30 天 = 趋势折线（柱状图在长区间会互相重叠不可读）；日期胶囊从图表数据首尾推导（不再从天数计算，杜绝不一致）；小结口径随区间自适应（本周/近N天）
- [x] 海报图表铺白色底（painter clearRect 会擦穿白卡，导出 PNG 透明区在相册显示为黑色）；y 轴刻度改整数步长（0/2/4/6/8 或 0/150/.../900）
- [x] 小结文案自动适配：放不下自动缩小字号（最低 9px），仍放不下截断加省略号（utils/canvasDraw fitText）
- [x] 三轮修复导出链路：rpx 缩水（画布尺寸改内联样式）、画布串台拉伸（海报固定绘制在专用隐藏画布）、页面残留（负偏移移出屏幕 + 整张位图导出，与 CSS 完全解耦）
- [x] **统计页 WHO 成长曲线拆为独立模块**（2026-09-17，用户反馈「UI 让人以为 WHO 曲线会随 7/15/30 天筛选生效」）
  - [x] 新增 `.who-section` 独立白卡，置于「成长趋势」上方；时间范围筛选条只留在下方趋势图模块内
  - [x] 标题行加「0-36 月龄」胶囊 + 说明文案，明确数据恒为全量、不随筛选变化
  - [x] 渲染条件与趋势图解耦：不再受 `displayDailyStats.length > 0` 约束 —— **附带修复「近 30 天无记录时 WHO 曲线连带隐藏」的问题**
  - [x] 身高/体重页签下下方模块标题「成长图表」→「成长趋势」
  - [x] 新增标题图标（三条平行短横线，取 WHO 百分位参考线意象），与趋势图折线图标区分
  - [x] 删除画布内重复标题（同日）：`GrowthCurveChart` 新增 `showTitle` prop，统计页传 `false` —— 去掉 Canvas 内嵌的「{宝宝名}的身高成长曲线 / WHO标准 · 0-36月龄」（与外层标题重复）；附带让绘图区顶部留白 48→16，曲线更高
  - [x] **补交互**（同日，用户反馈「图看不懂、缺交互」）
    - [x] `utils/whoGrowthStandards.ts` 新增 `getWhoPercentilesAt` / `getWhoBand`：按（小数）月龄线性插值出 7 个百分位，算某次测量落在哪两条参考线之间、与中位数差多少
    - [x] `GrowthCurveChart` 抽出 `getCurveLayout` / `filterCurvePoints`，**绘制与点击命中共用同一套坐标映射**
    - [x] 组件新增 `onSelectPoint`：点按曲线上的测量点即选中（36px 容差），选中态用绝对定位 DOM 圆环高亮、**不重绘画布**（否则打断生长动画）
    - [x] 统计页新增「怎么看」说明弹层：P50 不是及格线 / 看通道不看单点 / P3~P97 覆盖约 94% / 跨线下降才建议问医生（入口放在说明文字行右侧，避免标题行在系统大字号下溢出）
    - [x] 图表下方新增解读条：显示该次测量的月龄、数值、百分位区间、与中位数的差值
  - [x] **横轴缩放**（同日）：默认只展示最近 6 个月，支持双指捏合与 `−` `+` 按钮缩放
    - [x] 引入「视窗」模型 `CurveView{xMin,xMax}`，**横轴范围一变纵轴范围同步重算** —— 这是「横纵坐标一起变」的实现方式，不是两个独立 zoom
    - [x] `pickAgeTicks` 按视窗跨度自动切换横轴精度（0.5 / 1 / 2 / 3 / 6 / 12 月一档）；纵轴刻度精度同步跟随（身高范围 < 10cm 时保留一位小数）
    - [x] 跨度限制 1.5~36 个月、整体限制在 0~36 月龄；以视窗中心为锚点缩放
    - [x] 参考线整条绘制后按视窗裁剪（保留放大后的真实曲率）；标签贴视窗右边界，过近自动跳过
    - [x] 手势：`.gc-gesture` 蒙层接管触摸 + `catchMove` 阻止页面滚动；捏合抬手不误触发选中
    - [x] 性能：缓存 canvas `node/ctx`，尺寸已知时直接重绘，不再每帧走异步 `selectorQuery`
    - [ ] 待真机验收：**`catchMove` 是否真的阻止了页面滚动**（唯一平台风险点）、双指手势捕获、缩放帧率、点按命中精度、弹层在小屏不溢出、切换身高/体重后选中态清空、无记录空状态、海报分享入口
- [x] **统计页 6 张图支持点按联动明细**（2026-09-17）
  - [x] 柱状图 4 张（喂奶次数 / 奶量 / 尿布次数 / 睡眠时长）：点柱子 → 高亮 + 切换日期 → 上方日期导航与明细卡同步更新
  - [x] 折线图（身高体重趋势）：点数据点 → 数值气泡跟着走 —— **修掉了「只有末点有数值、中间的点读不出数」的问题**（14/30 天视图尤其明显）
  - [x] 体温图（**纯 DOM + ScrollView 手写，不是 LineChart 组件**）：数据点可点，选中点加深高亮
  - [x] 关键设计：高亮位置由 `selectedDate` 反查得出，**图表与日期导航天然双向同步**，不需要维护两份状态
  - [x] `BarChart` 复用已有 `.bc-mask`；`LineChart` 新加 `.lc-mask`（容器补 `position: relative`）
  - [x] 窄屏柱状图可横向滚动，靠 8px 位移阈值区分「滚动」与「点按」，不误触发
  - [x] **修误触**（同日用户反馈「一点到日期就被改了」）：命中区域由「整个槽位」收紧到「柱子 ± 14px」—— 槽位约 40px 而柱子只有 12px，原来点在柱子两侧的空白也算命中；`SCROLL_THRESHOLD` 8 → 12
  - [x] 日期导航加「一键回到今天」胶囊（固定位置，已是今天时置灰，避免日期变化时布局跳动）
  - [ ] 备选（若真机仍嫌误触）：改成两步式 —— 点柱子只高亮 + 显示数值，图表下方给「查看这天明细 ›」按钮，明确点击才切日期（零副作用，多一次点击）
  - [ ] 待真机验收：窄屏横向滚动时不应误触发选中、体温图数据点点击区域偏小（16px）、切换记录类型/时间范围后高亮是否正确重置

## 包体积与首屏性能（2026-09-17 起，09-18 修正方案）

- [x] **主包瘦身 1721 KB → 1527 KB**，剩余额度从 15.9% 提到 **25.5%**（共 14 张图改为 WebP）
  - [x] 根因：主包一半体积（873 KB）是 21 张装饰插画，其中 **5 张 PNG 占 395 KB** —— 照片/柔和渐变插画本不该用 PNG 存
  - [x] ⚠️ **09-17 曾用「透明区填页面底色 `#FFFAF9` 转 JPG」并上线，09-18 被真机反馈推翻**（喂奶页插画右上角变白方块）：页面背景是 `linear-gradient` 渐变，纯色对不上；且该方案依赖「背景永不改」，是脆弱假设。**已全部返工**
  - [x] 正解：**保透明 + WebP**（Pillow 生成，本机 ffmpeg/sips 都不支持 webp 编码）
    - album-baby 129→**19 KB**(q90) / sleep-baby 81→**32 KB**(q90)
    - edit-illustration 59→**55 KB**(无损) / feeding-baby 55→**45 KB**(无损) / app-logo 70→**13 KB**(q92)
    - ⚠️ **必须逐图择优**：有损 webp 对「原本就是调色板 PNG 的少色卡通插画」反而更大（edit-illustration 59→87 KB）
  - [x] 配套改 5 处 import（`pages/{photo,sleep,baby-edit,feeding,login}/index.tsx`）
  - [x] **09-18 追加：另外 9 张白底 JPG 插画也抠图改成 WebP**（用户反馈「换尿布页左上角图片也是白底」）
    - 根因：这批图**本来就是白底 JPG、没有 alpha**，放在彩色页面上就是一个白方块。真机采样确认白底 `(255,255,255)` vs 页面底色 `(254,250,250)` —— 只差 5 个色阶，但大面积浅色块相接时肉眼很明显
    - 扫描发现 15 张 JPG 里 **14 张都是白底插画**，但不是都要抠 —— 取决于**图片的直接容器背景**：
      `growth-baby-*` / `scale-baby-*` 在纯白卡 `.growth-hero-card { background: #fff }` 上**看不出来，未动**；`mini-program-code` 是二维码必须保留白底；`mine-pig` 是真实照片
    - 实际抠图 9 张：`diaper-baby` / `vaccine-safety` / `empty-baby` / `baby-bear-heart` / `family-illustration` / `avatar-boy` / `avatar-girl` / `baby-illustration` / `baby-illustration-girl`
    - 方法：连通性判断（只抠与边缘连通的底，不误伤衣服上的白）+ 反预乘去白边（`F=(P-(1-α)·BG)/α`）
    - ⚠️ **关键参数是 `alpha_quality` 不是 `quality`**：默认 100 时这批图会涨 183 KB（吃掉全部优化）；`quality=82, alpha_quality=68` 只涨 37 KB，而降到 50~70 肉眼在深灰底上放大也看不出差别
    - 配套改 8 处 import；主包 1491.5 → **1526.7 KB**（修 9 张图只多花 35 KB）
  - [x] 补 `apps/client/types/images.d.ts` 的 `declare module '*.webp'`（webpack 能过但 tsc 会报 TS2307）
  - [x] 验证：把原图与 webp 合成到各页真实底色上并排比对，透明区均正确透出、肉眼无差异
  - [x] 原有 15 张 JPG 已优化到位，sips 重压无收益，未动
  - [ ] ⚠️ **待真机验证（本次唯一平台风险）**：小程序 `<image>` 在 iOS 上加载 WebP 需 iOS 14+ / 基础库 2.9.0+。若不显示，回滚方式：`git checkout` 恢复 PNG + import 改回 `.png`
  - [ ] **分包：评估后决定不做** —— `subPackages[].root` 必须是独立目录，与主包页面同目录会冲突；要分包得移动 17 个页面目录 + 改所有相对 import + 改 20+ 处跳转，且无法在当前环境真机验证，风险远大于收益（约 400 KB）
  - [ ] 若还要再压：**图片走 CDN**（可再省 ~640 KB），但需先在微信后台配 `downloadFile` 域名白名单
- [x] **首页骨架屏**（修的不是「缺骨架」，是「每次冷启动都闪错误状态」）
  - [x] 根因：`babyStore` 没有持久化，冷启动 `currentBaby` 必为 null，于是**每次打开都先闪一下「还没有宝宝信息 + 去创建宝宝」**
  - [x] 加 `babyReady` 标志区分「加载中」与「真的没建档」，渲染改为三分支（骨架屏 / 空状态 / 宝宝卡）
  - [x] 骨架屏复用 `.baby-card` 等现有布局类，只换内部占位块 —— 数据回来时**不跳版**
  - [x] 顺带修第二处同类问题：「最近的瞬间」照片区加 `photosReady`，不再先闪「📷 拍照记录」
  - [ ] 待真机验收：冷启动不再闪空状态；已登录但**确实没有**宝宝档案的用户仍应正常看到空状态（这条最容易被改坏）

## 内容准确性修正（2026-09-14）

- [x] **疫苗计划从 2021 版升级到 2026 年版**（用户反馈「标准好像比较旧」）
  - [x] 依据《国家免疫规划疫苗儿童免疫程序及说明（2026年版）》（国疾控卫免发〔2026〕16号，2026-06-17 印发）
  - [x] 百白破：3/4/5 月龄 + 6 周岁白破 → **2/4/6 月龄 + 18 月龄 + 6 周岁**（共 5 剂，起始月龄提前到 2 月龄）
  - [x] 删除 6 周岁「白破疫苗」节点（2026 年版程序表已移除，DT 仅用于 7–11 周岁补种）
  - [x] 8 月龄「麻疹风疹联合疫苗」更正为「麻腮风疫苗 第1剂」（2021 年版起就已调整，之前用的是 2016 版内容）
  - [x] 脊灰名称细分灭活/减毒，并补「已完成 4 剂含 IPV 成分疫苗可不接种 4 岁 bOPV」说明
  - [x] 乙脑、甲肝补注减毒/灭活两种剂次方案
  - [x] 双价 HPV 疫苗（13 周岁女孩，2 剂间隔 6 个月）作为参考行加入疫苗表，但不进宝宝时间轴与提醒（超出 0–6 岁范围）
  - [x] 新增 `npm run check:vaccine`：校验前后端两份计划表（节点 ID/月龄/名称）不漂移
  - [x] 服务器执行 `docs/vaccine-schedule-2026.sql`（清理已下线的 dt-1 自定义日期）（2026-09-17 用户确认已执行）
  - [ ] 技术债：计划表在前后端各存一份（`packages/shared` 是空壳未接线），建议后续抽到共享包

## 一句话总结

记录闭环已经扎实，别再加记录功能。接下来三个月只做三件事：
**让用户回得来（订阅提醒）· 进得来（激活引导）· 带得动（邀请卡片 + 成长曲线 + 日报海报）**

---

## 产品优化分析记录（2026-09-09）

> 产品经理视角复盘：项目当前的主要矛盾不是功能数量，而是「激活率、回访理由、家庭协作、数据长期信任」还没有形成稳定闭环。后续优先优化用户路径和验证指标，暂缓继续扩充记录类型。

### 产品判断

- [ ] 将核心路径统一为：进入小程序 → 建立宝宝档案 → 记录第一条 → 获得反馈 → 被提醒回来 → 与家人共同使用
- [ ] 首页从“功能入口集合”收敛为“当前宝宝状态 + 记一笔主按钮 + 今日时间线 + 有价值提醒”
- [ ] 把“记录工具”逐步升级为“每天给家长反馈、提醒和安心感的育儿助手”

### P0：激活与首页体验

- [ ] 新用户登录后进入 30 秒建档流程；头像、出生身高体重等非必要信息延后补充
- [ ] 建档完成后只推荐一个下一步动作：记录第一条（喂奶 / 尿布 / 睡眠）
- [ ] 区分“示例数据”和“我的数据”，避免未登录或未建档用户误以为看到他人信息
- [ ] 首页增加最近使用的 3 个记录类型，并支持用户自定义常用入口
- [ ] “按上次继续”从记录页下沉到首页，减少重复填写
- [ ] 对当天长时间未记录提供温和提示，避免制造打卡焦虑

### P0：回访与订阅消息

- [ ] 完成疫苗、晚间回顾订阅模板配置及体验版 / 真机验收
- [ ] 建立三层唤回：高频晚间回顾、中频成长趋势、低频疫苗 / 月龄节点
- [ ] 每类提醒支持单独开启 / 关闭，并控制发送频率
- [ ] 消息内容突出可执行价值：今日摘要、趋势变化、下一步建议，而不是泛提醒
- [ ] 增加订阅授权 → 发送成功 → 消息点击 → 回到记录 / 时间轴的完整漏斗

### P1：统一数据口径与增长分析

- [ ] 统一“激活用户、活跃用户、留存用户”的定义，并在后台标注分母和时间范围
- [ ] 留存按注册后第 1 / 7 / 30 个自然日统计，区分“打开小程序”和“产生记录”
- [ ] 后台支持按是否建档、是否首条记录、是否加入家庭进行 cohort 分析
- [ ] 增加关键路径漏斗：登录 → 建档 → 首条记录 → 次日回访 → 家庭邀请 → 家人加入
- [ ] 以实验为单位记录版本、目标指标、观察周期和结论，避免只看总量

### P1：家庭协作与邀请转化

- [ ] 验收分享邀请卡的完整链路：分享、登录回流、确认加入、重复转发、过期 / 满员处理
- [x] **家庭成员可修改昵称**（2026-09-12 完成，用户建议）
  - [x] 新增 `family_member_aliases` 表（家庭创建者 + 目标用户唯一），备注名只在本家庭内生效，不改写用户全局微信昵称
  - [x] 新增 `PATCH /family/member/nickname`：仅家庭创建者可改其他成员；**谁都不能改自己**（自己的名字去「我的」页改），空值即恢复默认
  - [x] 备注名走微信内容安全检测，违规直接拦截（与宝宝名、用户昵称同一口径）
  - [x] 家庭成员页：列表展示「备注名 || 微信昵称」，点其他成员卡片弹出操作层（修改昵称 / 移除成员），输入弹层支持「恢复默认」；自己那一行不可点
  - [x] 移除成员 / 退出家庭时清理备注名
  - [x] 本地自测工具：`npm run check:family`（造一个「假家人」+ 13 项接口断言，解决本地没法真邀请第二个微信号的问题；清理用 `npm run check:family:clean`）
    - 带安全闸门：默认只放行 DB_HOST / API_BASE_URL 都指向本机的场景，指向线上直接中止
  - [x] 执行 `docs/family-member-nickname.sql` 建表（2026-09-17 用户确认已执行）
- [ ] 增加家庭成员角色和权限：只读、可记录、管理员
- [ ] 优化成员退出、重新加入、多宝宝和复杂家庭关系的处理
- [ ] 记录邀请曝光 → 点击 → 登录 → 加入成功的转化率及失败原因

### P1：长期数据信任

- [ ] 支持记录 CSV / Excel 导出
- [ ] 支持照片原图或批量下载
- [ ] 增加数据备份、换设备恢复和导出进度提示
- [ ] 补充账号注销、数据删除范围、删除后恢复规则
- [ ] 在隐私政策中明确照片、儿童健康信息、AI 分析数据的存储和第三方处理范围

### P1：让统计结果“看得懂”

- [ ] 图表下增加自然语言总结，例如平均奶量、睡眠变化、与上周期差异
- [ ] 对 WHO 曲线明确“参考范围，不代表医疗诊断”，避免用户误读百分位
- [ ] 统计页提供“下一步建议”：继续记录、补充测量、订阅提醒或分享给家人
- [ ] 观察成长曲线、日报海报的实际查看率、保存率、分享率和分享后回流率

### P2：相册、里程碑与 AI 闭环

- [ ] 相册从“照片文件夹”升级为按月龄 / 日期的成长时间线
- [ ] 增加月度精选、成长节点和纪念海报，提升情感留存
- [ ] AI 便便识别与尿布记录打通：记录尿布 → 可选上传 → AI 观察 → 自动回填
- [ ] 保存 AI 分析历史，并强化图片授权、隐私说明和医疗免责声明
- [ ] 辅食清单增加新食物 3 天观察和过敏标记，服务 6–12 月龄用户

### 90 天执行节奏

- [ ] 第 1–2 周：建档 / 首条记录流程、首页信息层级、埋点和留存口径
- [ ] 第 3–6 周：订阅消息、晚间回顾、邀请漏斗、家庭权限、数据导出
- [ ] 第 7–12 周：月度成长相册、里程碑、辅食观察、AI 与尿布记录打通

### 暂缓事项

- [ ] 暂不增加更多记录类型
- [ ] 暂不做会员 / 支付、社区 / UGC、独立 App、泛化 AI 育儿问答

### 建议关注指标

| 指标 | 当前关注 | 阶段目标 |
|---|---|---|
| 建档完成率 | 注册用户中完成宝宝档案的比例 | 持续提升，优先解决未建档流失 |
| 首条记录率 | 建档后 10 分钟内产生首条记录 | 作为激活核心指标 |
| 7 日留存 | 统一口径后重新建立基线 | 40%+ |
| 订阅授权 / 点击率 | 疫苗与晚间回顾分开统计 | 授权 50%+ / 点击 10%+ |
| 家庭邀请加入率 | 分享卡点击到成功加入 | 持续提升并拆解失败原因 |
| 日报 / 图表分享回流率 | 分享后再次打开小程序 | 上线后建立基线 |

### 一句话结论

**未来三个月优先做“让用户顺利开始、愿意回来、能和家人一起用、相信数据不会丢”，而不是继续堆叠功能。**
